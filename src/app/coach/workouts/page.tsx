import AppShell from "@/components/AppShell";
import WorkoutPlanFlow from "./WorkoutPlanFlow";

export default function WorkoutsPage() {
  return (
    <AppShell title="Workout plan">
      <WorkoutPlanFlow />
    </AppShell>
  );
}
